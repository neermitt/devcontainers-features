{
    "id": "kind",
    "version": "1.0.0",
    "name": "Kind",
    "description": "Installs latest version of kind (Kubernetes In Docker). Auto-detects latest versions and installs needed dependencies.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "none",
                "v0.15.0"
            ],
            "default": "latest",
            "description": "Select or enter a kind version to install"
        }
    }
}