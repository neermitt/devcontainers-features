{
    "id": "helmfile",
    "version": "1.0.0",
    "name": "Helmfile",
    "description": "Installs latest version of helmfile. Auto-detects latest versions and installs needed dependencies.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "none",
                "v0.146.0"
            ],
            "default": "latest",
            "description": "Select or enter a helmfile version to install"
        }
    }
}